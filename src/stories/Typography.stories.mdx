import {Meta, Typeset} from '@storybook/addon-docs/blocks';
import {primaryFont, secondaryFont} from "../components/styles";

<Meta title="Styles/Typography"/>

# Font & Typography
---
The project's typescale is set to Major Third with a base of 16px.

It uses DM Serif Display as the primary font, and it is applied to all the header elements. Paragraphs and smaller text use the Nunito font

### Headers

> h1, h2, h3, h4 & h5

<Typeset
    fontFamily={primaryFont}
    fontSizes={[
        String('48px'), String('39px'), String('31px'), String('25px'), String('20px')
    ]}
    sampleText="Elizabeth Design System"
    fontWeight={700}
/>

### Paragraphs, Copywright & Helper Text

> h1, h2, h3, h4 & h5

<Typeset
    fontFamily={secondaryFont}
    fontSizes={[
        String('16px'), String('12px'), String('10px')
    ]}
    sampleText="Elizabeth Design System"
    fontWeight={700}
/>
